This code could be used to read binary files which is formatted YUV420. The Y grey component is extracted and saved as png image.

The code depends on boost and opencv libraries which can be installed through ubuntu apt-get command. 
